apiVersion: 1

providers:
  # 암호화폐 대시보드 (사용자 직접 제작)
  - name: 'crypto-dashboards'
    orgId: 1
    folder: 'Crypto Tracker'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards/crypto